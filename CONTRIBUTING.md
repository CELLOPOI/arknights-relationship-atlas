# 参与开发

先阅读[开发指南](docs/DEVELOPMENT.md)、[资料维护流程](docs/EDITORIAL_WORKFLOW.md)和[仓库文件约定](docs/REPOSITORY_POLICY.md)。仓库提供本地开发、构建和验证需要的源码、基线及固定素材。

代码贡献从目的明确的短期分支发起 PR，关联适用 Issue，说明具体问题、改动后的行为、实际检查和必要迁移。默认执行 `make check`；涉及后端、模型或资料发布时，使用独立测试数据库执行 `make check-postgres`。交互变更按范围执行浏览器回归，注明模拟 API 或真实隔离数据库的验证边界；未执行的检查如实说明。SQLite 不能替代 PostgreSQL 的锁、并发和约束验收。

资料纠错优先提交资料 Issue，写明人物或关系、当前判定、建议修订及可定位的原文出处。截图需配合章节、档案或来源链接。保留知晓方向、异格身份和条件叙事边界，不将同阵营、同场景或游戏双向通路直接解释成相识。

资料编辑通过后台修订人物、关系和证据，经审核与发布写入正式版本。反馈先形成草稿，私有备注和联系方式不进入公开快照。Git 资料文件用于首次初始化及单向归档；撤下保留稳定 ID，回退生成新版本。代码迁移须保留已有资料，不能重复应用初始包覆盖后台维护结果。上游工具只生成审读材料和候选差异，日常开发不要求重建上游资料。

新业务代码使用 TypeScript 或 Python，沿用现有接口和视觉约束，文案遵循 [AGENTS.md](AGENTS.md)。工具依赖使用项目锁文件，避免个人全局依赖与绝对路径。注释说明原因和约束，避免无关格式化或依赖升级。

提交前执行 `git diff --cached --name-status`、`git diff --cached`、`git diff --cached --check`，逐项核对实际暂存内容，并按[提交前检查](docs/REPOSITORY_POLICY.md#提交前检查)检查提交信息、PR、Issue 及附件。个人主机/IP/SSH、绝对路径、备份与告警标识、订单等私有资料留在仓库外；示例使用回环或保留示例值。删除文件和忽略规则不会清除历史，历史清理后不得合并旧历史。

提交源文件、必要资料和清单匹配的固定素材；不提交凭据、运行数据库、真实反馈、邮件、备份、日志、缓存或构建输出。新增素材需同步来源和清单，不能将整个本地素材目录无差别加入提交。

原创代码采用 [MIT](LICENSE)，第三方内容保留来源、许可和版权声明，不将游戏素材、剧情引用或第三方代码改标为 MIT。进一步许可核对按[当前项目约定](AGENTS.md#当前许可核对范围)暂缓，已有状态见 [THIRD_PARTY.md](THIRD_PARTY.md)。分支、检查与审查职责见[项目管理](docs/PROJECT_MANAGEMENT.md)。
